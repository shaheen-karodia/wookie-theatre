import React from "react";
import "./standard-container.scss";

function StandardContainer({ children }) {
  return <div className="standard-container">{children}</div>;
}

export default StandardContainer;
